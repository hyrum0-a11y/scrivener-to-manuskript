# Python
__pycache__/
*.pyc

# Build artifacts regenerated by the scripts on every run — not source
reference.odt

# Real manuscript/book content and personal outputs never belong in this
# repo — Scrivener projects, converted vaults, and any generated exports.
*.scriv/
manuscript_chapters/
output/
*.epub
*.pdf
*.msk/

# Personal local-only tools — never published to the public repo (same
# treatment as piper_reader.py / kbd-backlight-setup.md, which are simply
# never git-added rather than pattern-matched here, since those predate
# this file). dashboard/'s config.py hardcodes real personal vault paths.
dashboard/
